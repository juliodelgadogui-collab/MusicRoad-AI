name: EPC Android 2.4.2 Sessão do Player

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo '2.4.2 validada com sucesso no run 33940214059.'
          echo 'Workflow arquivado para não validar versões 2.4.x futuras com marcadores antigos.'
