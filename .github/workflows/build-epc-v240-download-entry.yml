name: EPC Android 2.4 Download de Música Visível - manutenção

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow 2.4 concluído; builds seguintes usam a linha 2.4.x."
