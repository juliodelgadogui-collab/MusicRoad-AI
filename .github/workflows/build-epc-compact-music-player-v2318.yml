name: EPC Android 2.3.18 Player de Música Compacto - histórico

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow histórico 2.3.18 pausado; use o workflow da versão Android atual."
