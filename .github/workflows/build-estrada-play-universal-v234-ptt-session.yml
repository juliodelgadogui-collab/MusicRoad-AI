name: Estrada Play Universal 2.3.4 PTT Estavel Sessao Persistente - manutenção

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'Workflow histórico 2.3.4 arquivado.'
          echo 'A linha Android atual usa workflows dedicados 2.4.x.'
