name: EPC Android 2.3.15 Índice Local de Música - histórico

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow histórico 2.3.15 pausado; use o workflow da versão Android atual."
