name: EPC Android 2.3.9 Comboio Auto DB - manutenção

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'Workflow histórico 2.3.9 arquivado.'
          echo 'Comboio permanece no código consolidado; builds Android atuais usam a linha 2.4.x.'
