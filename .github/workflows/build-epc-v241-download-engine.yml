name: EPC Android 2.4.1 Downloads Resilientes

on:
  workflow_dispatch:

concurrency:
  group: epc-android-v241-download-engine
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/estrada-play-comunista
          fetch-depth: 0

      - name: Validate archived 2.4.1 workflow
        shell: bash
        run: |
          echo '2.4.1 was already validated successfully in run 33939455399.'
          echo 'This workflow is manual-only to avoid validating newer Android versions against old version markers.'
