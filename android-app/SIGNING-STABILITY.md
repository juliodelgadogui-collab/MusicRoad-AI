# MusicRoad stable APK signing

The Android debug APK workflow keeps a stable cached signing key from MusicRoad 4.2.0 onward so future test builds can update the installed app without changing its signing identity.
